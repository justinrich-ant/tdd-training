# DEMO 1 — this function does not exist yet.
# Spec: given a flag dict and a population size, return how many users
# would see the flag. Return 0 if the flag is disabled.
#
# After Demo 1 is live, the implementation will be here.


def rollout_reach(flag: dict, total_users: int) -> int:
    ...
